#include<stdio.h>
#include<math.h>

int main()
{
    long long int t,nr,np,i,j,k,l,dis,rad;
    scanf("%lld",&t);
    for(i=1;i<=t;i++)
    {
        scanf("%lld %lld",&nr,&np);
        long long int r[nr][3];
        long long int p[np][2];
        for(j=0;j<nr;j++)
        {
            scanf("%lld %lld %lld",&r[j][0],&r[j][1,&r[j][2]);
        }
        for(j=0;j<np;j++)
        {
            scanf("%lld %lld",&p[j][0],&p[j][1]);
            for(k=0;k<nr;k++)
            {

            }
        }
    }
    return 0;
}
